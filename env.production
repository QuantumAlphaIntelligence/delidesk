# Canal prod — versionado (sem secrets). Embutido no .exe via channel.json no CI.
# Dev local continua usando .env.local (gitignored).

DELIDESK_API_URL=https://api.delivai.com.br

DELIDESK_AUTH_URL=https://app.delivai.com.br/autorizar
DELIDESK_PANEL_URL=https://app.delivai.com.br/dashboard/orders
DELIDESK_CHAT_URL=https://app.delivai.com.br/dashboard/orders

# Instalador = OAuth real + poll de jobs
DELIDESK_AUTH_MOCK=0
