# Canal prod — versionado (sem secrets). Embutido no .exe via channel.json no CI.
# Dev local continua usando .env / .env.local (gitignored) — NÃO entra no instalador.

DELIDESK_API_URL=https://delivai-prod-delivai-backend.rwysej.easypanel.host

DELIDESK_AUTH_URL=https://delivaibot.com/autorizar
DELIDESK_PANEL_URL=https://delivaibot.com/dashboard/orders
DELIDESK_CHAT_URL=https://delivaibot.com/dashboard/conversations

# Instalador = OAuth real + poll de jobs
DELIDESK_AUTH_MOCK=0
