# Canal sandbox — versionado (sem secrets). Embutido no .exe via channel.json no CI.
# Dev local continua usando .env.local (gitignored).

DELIDESK_API_URL=https://delivai-test-delivai-backend-sandbox.rwysej.easypanel.host

# Front de teste público (mesmo host do painel). Ajuste se o front sandbox mudar.
DELIDESK_AUTH_URL=https://delivai.com.br/autorizar
DELIDESK_PANEL_URL=https://delivai.com.br/dashboard/orders
DELIDESK_CHAT_URL=https://delivai.com.br/dashboard/orders

# Instalador = OAuth real + poll de jobs
DELIDESK_AUTH_MOCK=0
